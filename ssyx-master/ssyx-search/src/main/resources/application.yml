server:
  port: 8083
  tomcat:
    uri-encoding: UTF-8
    threads:
      max: 800
      min-spare: 30

spring:
  application:
    name: ssyx-search
  profiles:
    active: dev

logging:
  level:
    com.zdf: debug
    org.springframework: warn